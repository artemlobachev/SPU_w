#include <stdio.h>

#include "assembler.h"

int main(int argc, const char * const argv[])
{
    //CheckInput();

    const char *AsmFile = argv[1];
    const char *BinFile = argv[2];

    //ReadFile();

    
}